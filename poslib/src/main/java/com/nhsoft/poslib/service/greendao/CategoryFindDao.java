package com.nhsoft.poslib.service.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.nhsoft.poslib.entity.nongmao.CategoryFind;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CATEGORY_FIND".
*/
public class CategoryFindDao extends AbstractDao<CategoryFind, Long> {

    public static final String TABLENAME = "CATEGORY_FIND";

    /**
     * Properties of entity CategoryFind.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "ID");
        public final static Property Branch_num = new Property(1, int.class, "branch_num", false, "BRANCH_NUM");
        public final static Property Merchant_num = new Property(2, int.class, "merchant_num", false, "MERCHANT_NUM");
        public final static Property Stall_item_category_code = new Property(3, String.class, "stall_item_category_code", false, "STALL_ITEM_CATEGORY_CODE");
        public final static Property Stall_item_category_name = new Property(4, String.class, "stall_item_category_name", false, "STALL_ITEM_CATEGORY_NAME");
        public final static Property Stall_item_category_num = new Property(5, String.class, "stall_item_category_num", false, "STALL_ITEM_CATEGORY_NUM");
        public final static Property Stall_item_category_rate = new Property(6, float.class, "stall_item_category_rate", false, "STALL_ITEM_CATEGORY_RATE");
        public final static Property Stall_item_category_sequence = new Property(7, int.class, "stall_item_category_sequence", false, "STALL_ITEM_CATEGORY_SEQUENCE");
        public final static Property Stall_num = new Property(8, int.class, "stall_num", false, "STALL_NUM");
        public final static Property System_book_code = new Property(9, String.class, "system_book_code", false, "SYSTEM_BOOK_CODE");
    }


    public CategoryFindDao(DaoConfig config) {
        super(config);
    }
    
    public CategoryFindDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CATEGORY_FIND\" (" + //
                "\"ID\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"BRANCH_NUM\" INTEGER NOT NULL ," + // 1: branch_num
                "\"MERCHANT_NUM\" INTEGER NOT NULL ," + // 2: merchant_num
                "\"STALL_ITEM_CATEGORY_CODE\" TEXT," + // 3: stall_item_category_code
                "\"STALL_ITEM_CATEGORY_NAME\" TEXT," + // 4: stall_item_category_name
                "\"STALL_ITEM_CATEGORY_NUM\" TEXT," + // 5: stall_item_category_num
                "\"STALL_ITEM_CATEGORY_RATE\" REAL NOT NULL ," + // 6: stall_item_category_rate
                "\"STALL_ITEM_CATEGORY_SEQUENCE\" INTEGER NOT NULL ," + // 7: stall_item_category_sequence
                "\"STALL_NUM\" INTEGER NOT NULL ," + // 8: stall_num
                "\"SYSTEM_BOOK_CODE\" TEXT);"); // 9: system_book_code
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CATEGORY_FIND\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CategoryFind entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getBranch_num());
        stmt.bindLong(3, entity.getMerchant_num());
 
        String stall_item_category_code = entity.getStall_item_category_code();
        if (stall_item_category_code != null) {
            stmt.bindString(4, stall_item_category_code);
        }
 
        String stall_item_category_name = entity.getStall_item_category_name();
        if (stall_item_category_name != null) {
            stmt.bindString(5, stall_item_category_name);
        }
 
        String stall_item_category_num = entity.getStall_item_category_num();
        if (stall_item_category_num != null) {
            stmt.bindString(6, stall_item_category_num);
        }
        stmt.bindDouble(7, entity.getStall_item_category_rate());
        stmt.bindLong(8, entity.getStall_item_category_sequence());
        stmt.bindLong(9, entity.getStall_num());
 
        String system_book_code = entity.getSystem_book_code();
        if (system_book_code != null) {
            stmt.bindString(10, system_book_code);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CategoryFind entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getBranch_num());
        stmt.bindLong(3, entity.getMerchant_num());
 
        String stall_item_category_code = entity.getStall_item_category_code();
        if (stall_item_category_code != null) {
            stmt.bindString(4, stall_item_category_code);
        }
 
        String stall_item_category_name = entity.getStall_item_category_name();
        if (stall_item_category_name != null) {
            stmt.bindString(5, stall_item_category_name);
        }
 
        String stall_item_category_num = entity.getStall_item_category_num();
        if (stall_item_category_num != null) {
            stmt.bindString(6, stall_item_category_num);
        }
        stmt.bindDouble(7, entity.getStall_item_category_rate());
        stmt.bindLong(8, entity.getStall_item_category_sequence());
        stmt.bindLong(9, entity.getStall_num());
 
        String system_book_code = entity.getSystem_book_code();
        if (system_book_code != null) {
            stmt.bindString(10, system_book_code);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CategoryFind readEntity(Cursor cursor, int offset) {
        CategoryFind entity = new CategoryFind( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // branch_num
            cursor.getInt(offset + 2), // merchant_num
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // stall_item_category_code
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // stall_item_category_name
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // stall_item_category_num
            cursor.getFloat(offset + 6), // stall_item_category_rate
            cursor.getInt(offset + 7), // stall_item_category_sequence
            cursor.getInt(offset + 8), // stall_num
            cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9) // system_book_code
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CategoryFind entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setBranch_num(cursor.getInt(offset + 1));
        entity.setMerchant_num(cursor.getInt(offset + 2));
        entity.setStall_item_category_code(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setStall_item_category_name(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setStall_item_category_num(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setStall_item_category_rate(cursor.getFloat(offset + 6));
        entity.setStall_item_category_sequence(cursor.getInt(offset + 7));
        entity.setStall_num(cursor.getInt(offset + 8));
        entity.setSystem_book_code(cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CategoryFind entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CategoryFind entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CategoryFind entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
