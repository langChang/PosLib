package com.nhsoft.poslib.service.greendao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.nhsoft.poslib.entity.PosItem;

import com.nhsoft.poslib.entity.PosItemTerminal;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "POS_ITEM_TERMINAL".
*/
public class PosItemTerminalDao extends AbstractDao<PosItemTerminal, Long> {

    public static final String TABLENAME = "POS_ITEM_TERMINAL";

    /**
     * Properties of entity PosItemTerminal.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Item_num = new Property(1, Long.class, "item_num", false, "ITEM_NUM");
        public final static Property Item_category_code = new Property(2, String.class, "item_category_code", false, "ITEM_CATEGORY_CODE");
    }

    private DaoSession daoSession;


    public PosItemTerminalDao(DaoConfig config) {
        super(config);
    }
    
    public PosItemTerminalDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"POS_ITEM_TERMINAL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ITEM_NUM\" INTEGER," + // 1: item_num
                "\"ITEM_CATEGORY_CODE\" TEXT);"); // 2: item_category_code
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"POS_ITEM_TERMINAL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PosItemTerminal entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long item_num = entity.getItem_num();
        if (item_num != null) {
            stmt.bindLong(2, item_num);
        }
 
        String item_category_code = entity.getItem_category_code();
        if (item_category_code != null) {
            stmt.bindString(3, item_category_code);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PosItemTerminal entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long item_num = entity.getItem_num();
        if (item_num != null) {
            stmt.bindLong(2, item_num);
        }
 
        String item_category_code = entity.getItem_category_code();
        if (item_category_code != null) {
            stmt.bindString(3, item_category_code);
        }
    }

    @Override
    protected final void attachEntity(PosItemTerminal entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PosItemTerminal readEntity(Cursor cursor, int offset) {
        PosItemTerminal entity = new PosItemTerminal( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // item_num
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // item_category_code
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PosItemTerminal entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setItem_num(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setItem_category_code(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PosItemTerminal entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PosItemTerminal entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PosItemTerminal entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPosItemDao().getAllColumns());
            builder.append(" FROM POS_ITEM_TERMINAL T");
            builder.append(" LEFT JOIN POS_ITEM T0 ON T.\"ITEM_NUM\"=T0.\"ITEM_NUM\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PosItemTerminal loadCurrentDeep(Cursor cursor, boolean lock) {
        PosItemTerminal entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PosItem posItem = loadCurrentOther(daoSession.getPosItemDao(), cursor, offset);
        entity.setPosItem(posItem);

        return entity;    
    }

    public PosItemTerminal loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PosItemTerminal> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PosItemTerminal> list = new ArrayList<PosItemTerminal>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PosItemTerminal> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PosItemTerminal> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
